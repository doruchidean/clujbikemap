<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:padding="16dp"
  >

  <NumberPicker
    android:id="@+id/picker_dialog_timer"
    android:layout_width="@dimen/drawer_picker_width"
    android:layout_height="wrap_content"
    android:layout_alignParentEnd="true"
    android:layout_alignParentRight="true"
    android:layout_centerVertical="true"
    android:layout_marginEnd="8dp"
    android:layout_marginRight="8dp"
    />

  <TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:drawableLeft="@drawable/generic_ic_timer"
    android:drawableStart="@drawable/generic_ic_timer"
    android:drawablePadding="16dp"
    android:layout_toLeftOf="@id/picker_dialog_timer"
    android:layout_toStartOf="@id/picker_dialog_timer"
    android:layout_alignParentStart="true"
    android:layout_alignParentLeft="true"
    android:layout_centerVertical="true"
    android:gravity="center_vertical"
    android:text="@string/dialog_timer_limit_title"
    android:textColor="@color/color_text_main"
    />

</RelativeLayout>